# LW39X0 Signal Capture V1.1 变更说明

本版本以用户提供的 V1.0 release 工程为唯一基线。

## 1. 存储链路

- 新增“写盘模式”：Auto / Buffered / Direct I/O。
- Auto 在 V1.1 中默认选择 Buffered / fwrite，写入粒度保持为单个 RX block（默认 256 KiB），更接近 `recv_demo`。
- Direct I/O 保留，使用 O_DIRECT，并继续按最大 8 MiB batch 写入。
- Buffered 模式运行过程中不主动每秒 `fflush`；Stop 时在 RX 资源释放之后执行最终 `fflush + fdatasync`，确保完成文件真正提交到底层存储。
- 应用层 RAM 缓存由“GiB 档位”改为“时间窗口”：Auto / 0.25 s / 0.50 s / 1.00 s / 2.00 s。
- Auto 目标约 0.75 s，最大 2 GiB；手动模式最大 4 GiB。
- 如果某个手动时长在当前采样率/通道数下需要超过主机安全内存或 4 GiB 保护上限，该选项自动禁用。
- 存储快速诊断由原启动阶段 200 ms / 稳态 1 s，调整为启动阶段 1 s / 稳态 2 s。
- 当持续写入明显低于 RX，且应用层缓存达到 85% 时，提前保护停止，不再等待 100% 填满，以减少停止后的积压写入时间。

### 8 通道 × 122.88 MS/s 示例

原始数据率约 3750 MiB/s：

- Auto：受 2 GiB 上限约束，实际约 0.55 s；
- 0.25 s：约 0.94 GiB；
- 0.50 s：约 1.83 GiB；
- 1.00 s：约 3.66 GiB；
- 2.00 s：约 7.32 GiB，超过 V1.1 的 4 GiB 手动上限，因此会禁用。

## 2. RF Calibration

新增“本次执行 RF 校准”开关，默认关闭。

关闭时：

- 本次 Start 不调用任何 `lw39x0_set_zone_*_calibrate()`。

开启时：

- LW3920：仅按需校准 Zone A；
- LW3940：按已使能通道校准 Zone A / B；
- LW3980：按已使能通道校准 Zone A / B / C / D；
- 校准发生在频率、采样率、通道、增益配置之后，`lw39x0_issue_recv_start()` 之前；
- 如果校准 API 阻塞期间点击 Stop，Stop 请求会被记录，并在当前校准 API 返回后执行。

## 3. 未在 V1.1 中做的改动

没有启用“lw39x0_recv 直接写入 Writer ring slot”的零拷贝方案。当前 SDK 文档只说明用户提供 buffer，但没有明确异步持有、DMA 生命周期和并发复用约束。为避免引入难以定位的 DMA/PCIe 数据错误，V1.1 继续保留 RX buffer -> writer queue 的一次内存复制。

也没有在每次 Start 前自动跑 SSD benchmark。短时间 benchmark 容易受 SLC cache、温度和冷盘状态影响，而且本身会加热 SSD。V1.1 继续使用“需求速率 + 实时 writer/proc write + queue + NVMe 温度”的在线判断。

## 4. 建议验收顺序

1. V1.1 Auto，8 通道 × 122.88 MS/s，冷盘启动，记录 30 s / 60 s 温度曲线。
2. 同条件运行原 `recv_demo`，比较温度与文件增长速率。
3. V1.1 Direct I/O 重复同样测试，隔离 I/O backend 影响。
4. 对三种模式分别检查最终文件大小、非零率、RMS、范围和运行日志。
5. 如果 FPGA/驱动能够提供递增计数器或已知测试序列，再增加样本级连续性验证；当前非零率/RMS 只能证明“有有效 IQ”，不能证明“绝对没有掉样”。
