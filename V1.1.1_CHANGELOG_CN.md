# LW39X0 Signal Capture V1.1.1 变更说明

V1.1.1 以 V1.1 为基线，只做日志产品化与停止后的离线数据校验；不修改 `lw39x0_recv()`、Writer 队列、Buffered/Direct I/O 和缓存保护等高速采集核心结构。

## 1. 日志模式

- 默认：普通模式。
  - GUI 只显示系统、设备、采集、文件完成、警告、错误和校验等关键事件。
  - 不创建运行日志文件。
  - 不连接开发者诊断信号。
  - RX Worker 不执行周期性的 `/proc/self/io`、`/proc/meminfo` 开发者采样，周期 IQ 诊断由 1 s 降为 5 s。
- 开发者模式。
  - GUI 显示普通事件和 `[DEV]` 详细诊断。
  - 自动保存完整日志到 `<程序目录>/log/`。
  - 最多保留最近 50 个开发者日志。

## 2. 数据校验

- `数据校验` 位于采集控制区域。
- Starting / Running / Stopping 时禁用，只在采集停止后可用。
- 自动绑定“上一次保存 IQ 的采集”，无需重新选择文件、采样率或通道。
- 每次保存 IQ 时，在 `<程序目录>/capture_meta/` 记录一个 JSON 元数据文件；不会改变 IQ `.bin` 的格式或路径。
- 用户点击数据校验后，由独立线程完整读取 IQ 文件，不占用实时 RX 路径。
- 校验内容：
  - 文件是否可完整读取、是否全零、是否按通道复样点组对齐；
  - RX 收到字节、Writer 入队字节、Writer 完成字节和最终文件字节的精确计数差；
  - 按 RX 有效时长、采样率和文件样本数估算每通道采样缺口和覆盖率；
  - Partial block 数量。
- 校验报告保存到 `<程序目录>/capture_meta/validation_run_*.txt`。

说明：当前 LW Driver / IQ 文件没有硬件 sample counter 或 block sequence，因此“应用链路字节差”可以精确计算，但“采样时长缺口”只能估算，无法精确定位缺口发生在哪个采样点。若后续驱动/FPGA提供 sample counter，可升级为严格的连续性验证。

## 3. 文件路径

- 原始 IQ：维持 V1.1 原逻辑，默认 `rx_iq.bin`（程序当前运行目录），仍可手动选择其它位置。
- 开发者日志：`<程序目录>/log/`。
- 采集元数据和校验报告：`<程序目录>/capture_meta/`。
- 截图默认保存到 `<程序目录>/screenshots/`，仍可在保存对话框中手动选择其它位置。
- 未新增其它自动输出目录。
